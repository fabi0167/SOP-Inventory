/*
* This is the model for the partType object. It is used to define the structure of the object.
* The partType object is used to store it in the database from frontend to backend.
*/

export interface PartType {
  id: number,
  partTypeName: string,
}
